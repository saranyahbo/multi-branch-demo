pipeline {
    agent any 
    stages {
        stage ("Test") {
            steps {
                script {
                    echo "This branch is $(BRANCH_NAME)"
                }
            }
        }
    }
}
